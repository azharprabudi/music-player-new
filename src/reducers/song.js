const INITIAL_STATE = {
  load: false,
  items: []
};

const SongReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default SongReducer;
